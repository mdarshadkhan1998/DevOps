**Meeting:** [Batch-43 4th Link | Live | Multi-Cloud with AWS + DevOps + AI | 8AM IST](https://app.tactiq.io/#/transcripts/8xtDuAQYozmzfA980KmH)

**Detailed stepwise summary (topics covered + commands used):**

- **1) Welcome & context**
  - Course/module: Multi-Cloud + DevOps + AI series; Module 5: Docker (4 days planned for Docker, then Kubernetes).
  - Goal: Add Docker to resume, learn Dockerfile, build/push images, run containers, and prepare for Kubernetes.

- **2) Why Docker? Problem statement vs Virtual Machines (VMs)**
  - VM characteristics discussed:
    - Full OS per VM, requires heavy hardware, larger disk footprint (example: VM images 10–30+ GB with apps/logs).
    - Advantages: multiple OS on one host, snapshots, easier testing across OS flavors, cost saving vs physical machines, faster provisioning vs buying hardware.
    - Disadvantages: heavy size, slower transport/collaboration, single point of failure on hypervisor, scaling overhead.
  - Docker/container benefits:
    - Lightweight images, portable, consistent runtime (pack binaries + libs + config), faster distribution, better for microservices.
    - Use-case: microservice architectures (front-end, product-catalog, payment, auth run as separate containers).

- **3) Core Docker concepts explained**
  - Image: packaged artifact containing binaries, libraries, OS layers relevant to the app.
  - Container: a running instance of an image (runtime). "Image becomes container when run."
  - Dockerfile: text blueprint to build images (keywords: FROM, RUN, COPY, ADD, WORKDIR, CMD, ENTRYPOINT, EXPOSE, MAINTAINER (deprecated), ENV, VOLUME, USER).
  - Registry / Docker Hub: public registry hosting millions of images (official vs community images; verified images have blue tick).
  - Docker daemon (dockerd), Docker client (CLI), Docker Hub — architecture flow: client -> daemon -> local images -> if not found -> Docker Hub -> daemon pulls -> daemon creates container -> streams output to client.

- **4) Image tags, sizes and common base images**
  - Tag concept: name:tag (e.g., ubuntu:22.04, hello-world:latest).
  - Size examples from demo:
    - ubuntu image: larger (multiple MBs/GB depending on flavor/tag)
    - centos example: larger (~300 MB referenced)
    - alpine: minimal (often ~5–15 MB depending on tag); very commonly used as small base image
  - Practical note: choose official/verified images in production; check last-updated and security maintenance.

- **5) Docker lifecycle & commands (demonstrated)**
  - Check Docker version:
    - docker -v
  - Check Docker service (systemd):
    - sudo systemctl status docker
  - List running containers:
    - docker ps
  - List all containers (including exited):
    - docker ps -a
  - List images on host:
    - docker images
  - Pull image from Docker Hub:
    - docker pull ubuntu
    - docker pull alpine
    - docker pull centos
  - Run container (pulls if absent and runs):
    - docker run hello-world
    - Example: docker run ubuntu (will start a container from ubuntu; to run interactively, see -it below)
  - Typical interactive/run flags shown or referenced:
    - docker run -it ubuntu /bin/bash   (interactive shell)
    - docker run -d --name myapp myimage  (detached/background)
  - Check image size after pull:
    - docker images    (shows REPOSITORY, TAG, IMAGE ID, SIZE)
  - Timing a command (to measure latency in demo):
    - time docker run hello-world
  - Notes about container exit behavior:
    - hello-world exits immediately because its task is to run and finish; to keep containers running use long-running process or run detached with proper command, or use interactive shells.

- **6) Installing Docker on VM (demo on GCP VM)**
  - Recommended to use cloud VM (GCP/AWS/Azure) for practice rather than local laptop unless resources adequate.
  - GCP Compute Engine steps used (via Cloud Shell / UI):
    - Console: console.cloud.google.com -> Compute Engine -> VM instances -> Create instance
    - Recommended VM config from class: Ubuntu LTS, 4 vCPU / 4 GB RAM (example), 30 GB disk (as practice default).
    - Use "Show equivalent REST/CLI" code (equivalent command) for automation.
  - Commands used from get.docker.com install script (demo):
    - curl -fsSL https://get.docker.com -o get-docker.sh
    - cat get-docker.sh         # inspect script
    - sh get-docker.sh         # execute installer (or sudo sh get-docker.sh)
    - apt-get update && apt-get install -y docker.io   # alternative (Ubuntu apt path mentioned)
  - After install, verify:
    - docker -v
    - sudo systemctl status docker
    - sudo usermod -aG docker $USER   # (optional) so docker can run without sudo (requires re-login)

- **7) Working with Docker Hub**
  - Search hub.docker.com for images (hello-world, ubuntu, centos, alpine).
  - Prefer official/verified images in production; check tags and last updated metadata.

- **8) Practical demo notes & instructor tips**
  - Use Cloud Shell in GCP to run VM creation commands and manage VMs for daily practice.
  - If an image isn't found locally, docker run will try docker pull automatically (and may ask for auth if private).
  - Use docker ps -a to inspect containers that have exited.
  - For production, avoid unverified/community images unless reviewed.
  - For interviews: be able to explain flow (client -> daemon -> registry) and why hello-world exits (it completes its task), and how to keep a container running.

- **9) Homework / expected practice**
  - Create a cloud VM (GCP or AWS) and install Docker with provided commands; share screenshot in WhatsApp group.
  - Pull images: ubuntu, centos (specific tag), alpine — note exact sizes and screenshots.
  - Review Dockerfile keywords and practice building a simple Dockerfile to create an image.
  - Target: create ~20 containers over next 4 days for hands-on comfort.

- **10) Useful online references (official docs)**
  - Docker installation and docs: https://docs.docker.com/get-docker/ and https://docs.docker.com/
  - Dockerfile reference: https://docs.docker.com/engine/reference/builder/
  - Docker CLI reference: https://docs.docker.com/engine/reference/commandline/cli/
  - Docker Hub: https://hub.docker.com/
  - GCP VM / Compute Engine docs: https://cloud.google.com/compute/docs
  - If any point in the session is unclear, refer to the above authoritative docs for exact flags, versions, and security guidance.


**Commands summary (copied / used during session):**
- VM creation (GCP Cloud Shell/equivalent CLI was used via UI): copy equivalent command from GCP UI.
- Docker install (demo):
  - curl -fsSL https://get.docker.com -o get-docker.sh
  - cat get-docker.sh
  - sh get-docker.sh
  - (alt) sudo apt-get update && sudo apt-get install -y docker.io
  - sudo usermod -aG docker $USER   # optional
- Inspect & status:
  - docker -v
  - sudo systemctl status docker
  - docker images
  - docker ps
  - docker ps -a
- Pull & run:
  - docker pull ubuntu
  - docker pull alpine
  - docker pull centos
  - docker run hello-world
  - docker run -it ubuntu /bin/bash   # interactive
  - docker run -d --name mycontainer myimage  # detached
- Timing a command:
  - time docker run hello-world


**10 scenario-based questions with answers (based on covered topics):**

1) Scenario: You ran docker run hello-world and the container immediately exited. Your manager asks why it didn’t keep running. Explain.
   - Answer: hello-world image executes a short program that prints a message and exits; containers stop when their main process ends. To keep a container running, run a long-running process (e.g., sleep infinity, system service) or start it interactively (docker run -it ubuntu /bin/bash) or run detached with a server process.

2) Scenario: You built an image and pushed to Docker Hub, but other team members can’t pull it (403/access denied). What could be the reason and remedy?
   - Answer: The image repository may be private — they need docker login with credentials that have pull access or the repository visibility must be set public OR you should add team members to the registry access policy. Also verify the correct image name/tag.

3) Scenario: Your team must run the same app across dev, QA, and prod ensuring identical runtime. Would you choose VM or container? Why?
   - Answer: Use containers (Docker) because images package app + dependencies and ensure consistency across environments; containers are lightweight and portable compared to full VMs.

4) Scenario: docker images shows two ubuntu images with different sizes (one ~2.5 GB, another ~119 MB). Explain why sizes differ and how to choose.
   - Answer: Different images/tags include different contents (full OS images vs slim/minimal variants). Larger images may include more packages, tools, or GUIs. For production, prefer minimal official images (e.g., ubuntu:20.04-slim or alpine) unless extra packages are intentionally included.

5) Scenario: You need an extremely small base image for a microservice binary. Which base image would you pick and why? Any caveats?
   - Answer: Alpine is commonly used for minimal size (very small, often single-digit MBs). Caveats: musl libc vs glibc differences can cause compatibility issues with some binaries; test thoroughly and consider distroless or slim variants if compatibility issues arise.

6) Scenario: After running docker run -it ubuntu /bin/bash, you install packages inside the container, exit, and later start the container again — you don’t see the installed packages. Why and how to persist?
   - Answer: If you created a container and didn’t commit changes to an image, starting a new container from the image won’t have the installed packages. Either commit the modified container to a new image (docker commit <container> newimage) or bake installations into a Dockerfile and build an image (docker build -t myimage .) so the changes persist.

7) Scenario: You need to run thousands of containers in production. Can you manage them with docker CLI only? What’s the recommended approach?
   - Answer: No — managing thousands of containers with CLI is impractical. Use orchestration/container management (Kubernetes, Docker Swarm, or other orchestration) to automate scheduling, scaling, service discovery and health checks.

8) Scenario: Your CI pipeline builds images and pushes to Docker Hub. You want to ensure the pipeline uses the latest base image patches for security. How to approach?
   - Answer: In the Dockerfile, pin base image tags carefully (e.g., use explicit tags), implement rebuild policies to rebuild images regularly, integrate Dependabot-like tools or automated rebuilds when base images are updated, and run vulnerability scanning (e.g., Trivy, Snyk) as part of CI.

9) Scenario: You pulled an image that runs but logs show permission errors when accessing /data mounted from host. How to fix?
   - Answer: Likely UID/GID mismatch between container process and host volume permissions. Fix options: adjust file ownership on host (chown), run container with appropriate UID/GID (--user), or use named volumes managed by Docker which set correct permissions. Also check SELinux/AppArmor contexts.

10) Scenario: Interview question — describe the flow when you run docker run <image> in an environment where the image is not local.
   - Answer: Docker client sends command to Docker daemon; daemon checks local image store; if image absent, daemon requests image from configured registry (default Docker Hub). Daemon pulls image layers, stores them locally, then creates a container from that image and starts it. Daemon streams stdout/stderr back to the client.

